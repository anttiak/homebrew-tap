# homebrew-tap

Homebrew tap for [anttiak](https://github.com/anttiak)'s apps.

## LoFeline

```sh
brew install --cask anttiak/tap/lofeline
```

The cask clears macOS quarantine on install, since the app isn't notarized.
